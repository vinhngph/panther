{
  "name": "panther",
  "version": "1.0.0",
  "description": "Access and download documents from Studocu (Studeersnel) without requiring an account.",
  "license": "MIT",
  "author": "Nguyen Phuc Vinh",
  "type": "commonjs",
  "main": "background.js",
  "scripts": {
    "build": "rm -rf ./pack/* && zip -r ./pack/panther.zip src/ manifest.json LICENSE && unzip ./pack/panther.zip -d ./pack/panther"
  },
  "dependencies": {
    "chrome-types": "^0.1.338"
  }
}
